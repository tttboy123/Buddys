services:
  - type: web
    name: buddys-console
    env: python
    plan: starter
    buildCommand: pip install .
    startCommand: PYTHONPATH=src uvicorn buddys_api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: BUDDYS_DEFAULT_MODEL
        value: MiniMax-M3
